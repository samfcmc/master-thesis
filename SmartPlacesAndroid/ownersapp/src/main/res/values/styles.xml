<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar"></style>

    <style name="Toolbar" parent="AppTheme">
        <item name="android:textColorPrimary">@color/toolbar_text</item>
        <item name="android:textColorSecondary">@color/menu</item>
        <item name="android:background">@color/toolbar_background</item>
    </style>

    <style name="ActionBar" parent="android:Widget.Holo.Light.ActionBar.Solid.Inverse"></style>

    <style name="AppThemeWithoutActionBarTitle" parent="AppTheme">
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="ActionBarWithoutTitle" parent="@style/ActionBar">
        <item name="android:displayOptions">useLogo|showHome</item>
    </style>

    <style name="deleteButton">
        <item name="android:background">@color/deleteBackground</item>
        <item name="android:textColor">@color/deleteText</item>
    </style>

    <color name="toolbar_background">#ff5ec8ff</color>
    <color name="toolbar_text">#ffffff</color>
    <color name="menu">#000000</color>
    <color name="deleteBackground">#ffd24f44</color>
    <color name="deleteText">#ffffff</color>

</resources>
